# Node RPC (WebSocket for live updates)
rpc_url = "ws://127.0.0.1:16110"

# P2P Settings
p2p_port = 16110
p2p_bootstrap_peers = ["127.0.0.1:16110"]

# Stitch Parameters
stitch_reward_sompi = 100000      # 0.001 KAS
dag_window = 10000
min_blue_delta = 500
rate_limit_seconds = 30
